[pytest]
# Only the files directly under tests/ are pytest suites. Everything in
# tests/scripts/ is a set of argparse CLI tools (batch runs, validation
# reports); several define a helper named test_condition() that pytest
# would otherwise try to collect as a test and fail on its arguments.
testpaths = tests
norecursedirs = scripts outputs venv scratch
